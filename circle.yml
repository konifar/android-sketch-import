dependencies:
  cache_directories:
    - sketchtool
  override:
    - script/install-sketchtool.sh

test:
  override:
    - script/generate-png.sh "$CIRCLE_ARTIFACTS"

deployment:
  master:
    branch: master
    commands:
      - script/import-from_sketch.sh ${CIRCLECI_TOKEN} ${CIRCLE_BUILD_NO} ${GITHUB_TOKEN}
